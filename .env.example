# Reference only: the scripts do not automatically load this file.
# Set these variables in the shell/kernel or use Colab Secrets.
OPENAI_API_KEY=your_openai_api_key_here
# Only needed for the optional Anthropic RAGAS cells:
ANTHROPIC_API_KEY=your_anthropic_api_key_here
